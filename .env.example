# DATABASE LAYER CONNECTION LINK (Update with your credentials or Render Postgres link)
# Format: postgresql://[user]:[password]@[host]:[port]/[database_name]
DATABASE_URL=sqlite:///./hardwareattendance.db

# API ENDPOINT TARGET
API_BASE_URL=http://localhost:8000

# SECURITY CONFIGURATIONS (Generate a strong secret key for production)
JWT_SECRET=your_jwt_secret_key_here
